version: "3"
services:
  tars:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - ./outputs/:/app/outputs
    ports:
      - 3880:80
    environment:
      TZ: Asia/Tokyo
    env_file:
      - .env